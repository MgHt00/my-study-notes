## Syntax Template:
```javascript
switch (expression) {
  case value1:
    // Code block for when expression === value1
    break;

  case value2:
    // Code block for when expression === value2
    break;

  case value3:
    // Code block for when expression === value3
    break;

  default:
    // Code block for when none of the cases match
    break;
}
```
